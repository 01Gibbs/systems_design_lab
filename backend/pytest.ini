# Enable pytest to find tests in src/app
[pytest]
testpaths = tests
python_files = test_*.py
addopts = --cov=src/app --cov-report=term-missing --cov-fail-under=85
markers =
    integration: marks tests as integration tests (deselect with '-m "not integration"')
